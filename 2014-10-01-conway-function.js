/**
 * Conway
 *
 * GIVEN: a cell and an array made up of the state of its neighbors
 * WHEN: called with `conway(cell, neighbors)`
 * THEN: Returns the state of cell after 1 tick
 */
/** === PRODUCTION CODE === **/
function conway(cell, neighbors) {

}


/** === TEST CODE === **/
console.log('It should return state of a cell after 1 tick: ',
  conway(cell, neighbors));